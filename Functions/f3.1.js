var students = [80,77,88,95,68];

var avrageMarks = 0;
function mid(avg, students){
    for (var i= 0; i < students.length; i++){
        return avrageMarks+=students;
        var avg = avrageMarks / students.length;
        
        console.log(avg);
    }
}
